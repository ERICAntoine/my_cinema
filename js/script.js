document.addEventListener("DOMContentLoaded", function(e)
{
    const abonnements = document.getElementById("abo");
    const addSelect = document.createElement("select");
    const abo = document.createElement("option");
    let text = document.createTextNode("VIP");
    abo.appendChild(text);
    text = document.createTextNode("GOLD");
    const ab


});